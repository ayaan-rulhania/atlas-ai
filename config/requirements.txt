# Minimal dependencies for Vercel deployment (lite_app.py)
# Only includes what's actually needed for the serverless function
# Updated: 2026-01-03 (v2.1.0)

# Web Framework
Flask>=3.1.0,<4.0.0
flask-cors>=4.1.0,<5.0.0

# Web Scraping & HTTP (for research engine)
requests>=2.32.0,<3.0.0
beautifulsoup4>=4.12.3,<5.0.0
lxml>=5.3.0,<6.0.0

# Image Processing (for image handler)
Pillow>=11.0.0,<12.0.0

# Utilities
python-dotenv>=1.0.1,<2.0.0

# Note: Heavy ML dependencies (torch, transformers, etc.) are NOT included
# as they are not used by lite_app.py and would exceed Vercel's limits

